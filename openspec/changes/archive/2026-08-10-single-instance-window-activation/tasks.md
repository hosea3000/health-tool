## 1. 单实例启动

- [x] 1.1 在 Wails 应用配置中启用稳定应用 ID 的 `SingleInstanceLock`，让后续实例由框架拦截并退出。
- [x] 1.2 注册第二实例回调，复用已有 `App` 上下文，不创建新的监控、提醒或时间线状态。

## 2. 唤醒主窗口

- [x] 2.1 实现已有实例的窗口唤醒逻辑：先取消最小化，再显示隐藏的主窗口；忽略第二实例参数。
- [x] 2.2 保持现有关闭窗口隐藏和 Windows 托盘“打开主界面”行为不变，并检查非 Windows 构建路径。

## 3. 验证

- [x] 3.1 补充或调整启动配置相关测试，验证单实例回调不会重建应用状态。
- [x] 3.2 运行 `go test ./...`，并在可用桌面环境验证第二次启动会唤醒已有主窗口而不是创建新实例。
