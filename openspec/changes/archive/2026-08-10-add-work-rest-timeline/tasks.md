## 1. 后端时间线模型

- [x] 1.1 在应用层定义时间线记录类型，包含 `working`、`resting`、`idle-paused`、开始时间、可选结束时间和持续秒数。
- [x] 1.2 在 `App` 中维护当前运行周期的时间线，并在工作段、闲置暂停、提醒休息期的状态转换边界关闭旧记录和创建新记录。
- [x] 1.3 新增只读 `Timeline()` Wails 方法，返回按开始时间排序的快照，并正确计算进行中记录的持续秒数。

## 2. 后端验证

- [x] 2.1 为有效活动开始工作段、闲置暂停、提醒休息期、休息结束和空时间线补充 Go 测试。
- [x] 2.2 验证现有 `go test ./...` 通过，且时间线功能不改变原有提醒和状态转换测试结果。

## 3. 前端展示

- [x] 3.1 扩展 Wails 前端绑定调用，获取当前运行周期时间线并在主界面增加垂直时间线区域。
- [x] 3.2 展示工作与暂停类型、时间范围、持续时长和进行中状态，并为无记录情况提供空状态。
- [x] 3.3 沿用现有每秒刷新机制更新时间线，保持浏览器预览模式可用，并运行 `cd frontend && npm run build` 验证构建。
