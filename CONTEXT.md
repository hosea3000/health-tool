# 健康工具箱

这是应用领域词汇表，覆盖多个工具。

## 久坐提醒

帮助用户在持续工作后起身活动的健康工具。它根据用户是否持续操作电脑来划分工作段，并在工作段达到提醒条件时暂停计时。

**有效活动**：键盘按键、鼠标点击、滚轮或明显的鼠标移动；不包括无法代表用户主动操作的微小鼠标抖动。

**工作段**：从一次有效活动开始，到进入暂停为止的一段连续工作时间。工作段达到工作段提醒时长时触发一次久坐提醒。

**工作段提醒时长**：工作段达到后触发久坐提醒的时长；默认 60 分钟，可在 1–180 分钟范围内按 5 分钟调整。设置从下一个工作段开始生效。

**待工作**：工具启动或用户继续后、尚未发生首次有效活动的状态；待工作不计入工作段。

**闲置暂停**：连续 5 分钟没有有效活动后进入的暂停状态；闲置时间不计入工作段。

**静默记录**：久坐提醒通知开关关闭期间的模式。监控与时间线照常运转，但工作段达到工作段提醒时长后不进入提醒休息期、不发送久坐提醒，工作段持续计时直至真实的闲置或退出；自动开始通知同样不发送；重新开启后本轮从当前时刻重新计满提醒时长。

**提醒休息期**：工作段达到工作段提醒时长并发出久坐提醒后进入的休息状态。默认持续 3 分钟，休息期内的输入不算有效活动。

**自动恢复**：工具启动、闲置暂停结束或提醒休息期结束后，首次有效活动自动创建新的工作段。

**自动开始通知**：新的工作段由有效活动自动创建时发出的一次性通知，内容包含本轮工作段提醒时长。

**托盘驻留**：主界面被关闭但工具仍在运行的状态；托盘驻留期间继续监控有效活动并发送提醒。

## 倒数日

展示一组可配置的倒计时事件的工具；每个事件对应一张 dashboard 卡片，卡片展示事件标题、规则文案、天数大数字与下一次到期日。

**倒数日事件**：用户在倒数日工具中配置的一个倒计时实例，由事件标题与到期规则构成。

**事件标题**：用户为倒数日事件命名的简短标题（不超过 20 字），用于在卡片和列表中识别该事件。

**到期规则**：决定下一次到期日的重复方式，包括一次性具体日期、每月固定日、每周固定周几、大小周固定周几四种。

**大小周**：每两周一次的到期规则。以创建日所在周为大周为锚，事件可选落在"大周"（本周开始）或"小周"（下周开始）。

**剩余天数**：下一次到期日与今天之间的天数；今天到期为 0，一次性日期已过则为负数，以"还剩 N 天 / 今天 / 已经 N 天"三段语义展示。

_Avoid_: 用"倒数日"指代单个事件（易与工具名混淆，应使用"倒数日事件"）；用"过期"指代到期规则的一次性日期（一次性日期不存在重复，用"已过"）

## 计数器

记录重复性事件次数的健康工具；每个计数器对应一张 dashboard 卡片，卡片右上角 + 按钮一键 +1，详情页可增删改并精确调整次数。

**计数器**：用户配置的一个计数实例，由名称、重置周期、可选目标值与次数桶构成。

**重置周期**：计数器清零的频率，四种：每天 / 每月 / 每年 / 永不清零。次数按"当前时间 + 周期"写入对应的计数桶，周期切换后当前次数从 0 重新累计。

**计数桶**：按周期划分的次数容器，key 为周期键（天→"2026-08-13"、月→"2026-08"、年→"2026"、永不清零→"all"）。历史桶保留，供详情页回顾最近 7 个非零周期。

**目标值**：可选的次数上限；设置后卡片展示"还差 N 次 / 已达成"，未设置则只计数、不设上限。

_Avoid_: 用"清零"指代周期切换逻辑（不存在主动清零动作，是周期键变化自然指向新桶）。
